pipeline {
    agent any
    stages {
        stage('Install dependencies') {
            steps {
                script {
                    sh('npm install')
                }
            }
        }
        stage('Unit Test') {
            steps {
                script {
                    sh('echo \"No test specified\" && exit 0')
                }
            }
        }
        stage('Build application') {
            steps {
                script {
                    sh ('echo \"No test specified\" && exit 0')
                }
            }
        }
    }
}
